<?xml version="1.0" encoding="utf-8"?>
<Configuration>

    <Appenders>
        <RollingFile name="file" fileName="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}jev-dev.log"
                 filePattern="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}jev-dev-%i.log">
            <PatternLayout pattern="%-5p %d [%t] [processor: %X{processorPath}; event: %X{correlationId}] %c: %m%n"/>
            <SizeBasedTriggeringPolicy size="10 MB"/>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>
        <!-- Console so the demo loggers are visible when running from Studio or `mvn mule:run`. -->
        <Console name="console" target="SYSTEM_OUT">
            <PatternLayout pattern="%-5p %d{HH:mm:ss} %c: %m%n"/>
        </Console>
    </Appenders>

    <Loggers>
        <!-- Http Logger shows wire traffic on DEBUG -->
        <!--AsyncLogger name="org.mule.service.http.impl.service.HttpMessageLogger" level="DEBUG"/-->
        <AsyncLogger name="org.mule.service.http" level="WARN"/>
        <AsyncLogger name="org.mule.extension.http" level="WARN"/>

        <!-- The demo's own flow loggers (category="jev.demo"). -->
        <AsyncLogger name="jev.demo" level="INFO"/>
        <!-- Turn this to DEBUG to see the connector's internal decisions and retries. -->
        <AsyncLogger name="com.mulesoft.connectors.jev" level="INFO"/>

        <!-- Mule logger -->
        <AsyncLogger name="org.mule.runtime.core.internal.processor.LoggerMessageProcessor" level="INFO"/>

        <AsyncRoot level="INFO">
            <AppenderRef ref="file"/>
            <AppenderRef ref="console"/>
        </AsyncRoot>
    </Loggers>

</Configuration>
